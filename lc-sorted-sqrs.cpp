class Solution {
public:
  vector<int> sortedSquares(vector<int> &nums) {
    // split by middle(where there's a negative next to positive)
    // Square both arrays
    // reverse left side(negatives)
    // merge smaller into large one
  }
};
